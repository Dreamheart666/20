#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <windows.h>
#include<string.h>
#include <math.h>
#include<windows.h>
int main()
{
	int a, b;
	int c = 1;
	scanf("%d %d", &a, &b);
	while (c != 0)
	{
		c = a%b;
		a = b;
		b = c;
	}
	printf("%d\n", a);
	return 0;
}
